# Plan Location

The canonical project plan now lives at the `TVD-Scheduler` project root:

- `../plan.md` in the deployed remote tree

This `code/plan.md` file is kept only as a pointer so the experiment plan has one authoritative location.
