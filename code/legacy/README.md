# Legacy Archive

This directory is for retired local entrypoints and compatibility shims that are no longer on the active diffusion-flow time reparameterization path.

The project-level archive under `../../legacy/diffusion_flow_reparameterization_cleanup_20260429T180424Z/` contains the moved canonical TVD runner, support utilities, old plans, result folders, and non-core extras with checksums in `archive_manifest.json`.

Active fixed-schedule evaluations should use `../diffusion_flow_time_reparameterization.py`.
